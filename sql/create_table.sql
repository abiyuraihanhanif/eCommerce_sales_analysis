CREATE TABLE ecommerce_sales (
    order_date DATE,
    product_name VARCHAR(255),
    category VARCHAR(100),
    region VARCHAR(100),
    quantity INTEGER,
    sales NUMERIC(12,2),
    profit NUMERIC(12,2)
);